@page "/Ej38"
@rendermode InteractiveServer

<PageTitle>Ejercicio 38</PageTitle>

<h3>Ejercicio 38</h3>

<p>Construir una función que reciba como parámetro un vector de 10 posiciones enteras y retorne la cantidad de números terminados en 3 que contiene el vector.
</p>

<div>
    <label>Ingresa 10 números enteros separados por comas:</label>
    <input type="text" @bind="inputVector" />
</div>

<div class="form-group">
    <button class="btn btn-primary" @onclick="Calcular">Calcular</button>
</div>

<div class="form-group">
    Cantidad de números terminados en 3: @resultado
</div>

@code{
    private string inputVector = "";
    private string resultado = "";

    private void Calcular(){
        var numeros = inputVector.Split(',').Select(int.Parse).ToArray();
        resultado = numeros.Count(n => n % 10 == 3).ToString();  
    }
}
